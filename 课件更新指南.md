# 📖 HTML课件库 - 课件更新指南

## 🎯 使用方式说明

本项目只需在 **一台电脑**（开发电脑）上维护课件，推送到 GitHub 后，
**任何电脑**通过网页地址即可访问最新课件，无需安装任何软件、无需 git。

> 🔗 网页访问地址：`https://你的用户名.github.io/仓库名/`
> （在 GitHub 仓库 → Settings → Pages 中启用即可获得此地址）

## 一、新课件添加流程

### 方式A：通过管理后台（推荐）

1. 打开 `manage.html` 管理后台页面
2. 填写课件名称、选择年级、填写学科
3. 选择你的 `.html` 课件文件
4. 点击 **「生成 ZIP 并下载」**，浏览器会下载一个 ZIP 包
5. 解压 ZIP 包，把解压出的文件夹放到 `courses/对应年级/` 目录下
6. 双击运行根目录的 **`generate.bat`** 更新索引
7. Git 提交并推送：

```
git add .
git commit -m "新增课件：课件名称"
git push
```

### 方式B：手动创建

1. 在 `courses/年级/` 下新建课件文件夹（如 `courses/grade3/my-course/`）
2. 放入 `index.html` 课件文件
3. 在同目录创建 `meta.json`，格式如下：

```json
{
  "title": "课件标题",
  "subject": "学科名称",
  "createdAt": "2026-06-15"
}
```

4. 双击运行根目录的 **`generate.bat`** 更新索引
5. Git 提交并推送

---

## 二、修改已有课件

### 修改课件内容（HTML文件）

- 直接编辑 `courses/年级/课件文件夹/index.html` 即可
- HTML 文件修改后 **无需重新运行 generate.bat**（文件路径未变）

### 修改课件信息（标题、学科等）

- 编辑 `courses/年级/课件文件夹/meta.json`
- 修改后 **需要运行 generate.bat** 更新索引

---

## 三、删除课件

1. 删除 `courses/年级/课件文件夹` 整个目录
2. 运行 `generate.bat` 更新索引
3. Git 提交并推送

---

## 四、添加新年级

编辑 `generate.py` 文件中的 `GRADES_CONFIG` 列表，按格式添加：

```python
{"id": "grade7", "name": "七年级", "description": "新的征程"},
```

然后运行 `generate.bat`。

---

## 五、另一台电脑同步

**不需要任何操作！** 直接在浏览器打开网页地址即可看到最新课件：

```
https://你的用户名.github.io/仓库名/
```

> 只要开发电脑 push 了更新，GitHub Pages 会自动在 1~2 分钟内刷新网页内容。

---

## 🚀 首次启用 GitHub Pages（只需配置一次）

1. 打开你的 GitHub 仓库页面
2. 点击 **Settings** → 左侧 **Pages**
3. **Source** 选择 `main` 分支，目录选 `/ (root)`，点击 **Save**
4. 等待 1~2 分钟，页面会显示你的网址：`https://xxx.github.io/xxx/`
5. 把这个网址收藏到浏览器，以后在任何电脑打开即可

---

## 七、完整工作流总结

```
┌────────────────────┐       git push        ┌──────────────┐
│   开发电脑          │ ──────────────────────→ │   GitHub      │
│                    │                         │              │
│ ① 编辑课件 HTML     │                         │ ② 自动部署    │
│ ② 运行 generate.bat│                         │   GitHub Pages│
│ ③ git push         │                         └──────┬───────┘
└────────────────────┘                                │
                                                       │ ③ 任何电脑
                                                       │ 浏览器打开网址
                                                       │ 即可看到最新课件
                                                       ↓
                                              🌐 任何电脑/平板/手机
---

## 六、常用 Git 命令速查

```bash
# 查看变更状态
git status

# 提交所有变更
git add .
git commit -m "更新说明"

# 推送到 GitHub
git push

# 从 GitHub 拉取最新
git pull
```

---

## ⚠️ 注意事项

- **每次增删课件文件夹后，务必运行 `generate.bat`**，否则首页和管理后台不会显示变更
- 课件文件夹名建议使用英文或拼音（如 `bubble-sort`、`ai-intro`）
- `meta.json` 的 `createdAt` 格式为 `YYYY-MM-DD`
- 所有课件都是纯文件，Git 可以完整同步，不再依赖浏览器本地存储
